package com.clickretina.skillforge.theme

import androidx.compose.ui.graphics.Color

// --- Exact reference colors from assignment HTML ---
// Background
val Cream        = Color(0xFFFBFAF8)   // #FBFAF8 — exact from assignment

// Brand teal
val Teal700      = Color(0xFF0FB5A4)   // #0FB5A4 — "See all" link & CTA
val Teal500      = Color(0xFF14B8A6)   // #14B8A6 — BEGINNER badge text
val Teal100      = Color(0xFFCCFBF1)   // FREE pill background
val TealIcon     = Color(0xFF2DD4BF)   // #2dd4bf — icon squares, progress bar

// Text
val TextPrimary   = Color(0xFF1A1A1A)  // #1A1A1A
val TextSecondary = Color(0xFF9A9890)  // #9A9890 — secondary labels
val TextMuted     = Color(0xFF6B6B62)  // #6B6B62 — rating/duration on cards

// Surfaces & borders
val SurfaceWhite = Color(0xFFFFFFFF)
val CardBorder   = Color(0xFFECEBE6)  // #ECEBE6 — card borders
val DividerGray  = Color(0xFFECEBE6)  // same as border for consistency

// Level badges
val LevelBeginner    = Color(0xFF14B8A6)   // teal for Beginner
val LevelIntermediate = Color(0xFFE0992B)  // amber for Intermediate
val LevelAdvanced    = Color(0xFFEF4444)   // red for Advanced

// Lesson pills
val FreePillBg   = Color(0xFFCCFBF1)
val FreePillText = Color(0xFF0F766E)
val LockedPillBg = Color(0xFFF3F4F6)
val LockedPillText = Color(0xFF9A9890)

// Star rating
val StarColor = Color(0xFFF5A623)   // #F5A623 — exact from SVG fill in reference

